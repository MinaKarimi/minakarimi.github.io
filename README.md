# MinaKarimi.github.io

Academic website for Mina Karimi.

## GitHub Pages
Create a repository named `MinaKarimi.github.io`, upload `index.html` and `assets/`, then enable **Settings → Pages → Deploy from branch → main / root**.

Add the finalized CV as `assets/Mina-Karimi-CV.pdf` when ready.
